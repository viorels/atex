# sudo adduser atex
# sudo apt-get update
# sudo aptitude install redis-server libmemcached-dev postgresql libpq-dev python-virtualenv python-dev libjpeg8-dev libapache2-mod-wsgi git

# a2dismod python perl php5

# sudo su - postgres
$ createdb atex

$ sudo su - atex
$ git clone https://viorels@bitbucket.org/viorels/atex.git
$ virtualenv venv
$ . venv/bin/activate
$ pip install -r atex/requirements/production.txt

$ cat << _EOF_ > .env
ANCORA_URI=http://ANCORA.SERVER:8765/ancoraerp/jis.serv?database=atex
MEMCACHE_SERVERS=localhost
DJANGO_SETTINGS_MODULE=atexpc.config.production
_EOF_

# echo "vm.overcommit_memory = 1" >> /etc/sysctl.conf

# wget -O /etc/init.d/celeryd https://raw.github.com/celery/celery/3.1/extra/generic-init.d/celeryd
# chmod +x /etc/init.d/celeryd
# ln -s atexpc/config/celeryd.default /etc/default/celeryd

# DEVELOPMENT

$ env `xargs < .env` python manage.py runserver		# run server

$ env `xargs < .env` celery -A atexpc worker -l info 	# run Celery

